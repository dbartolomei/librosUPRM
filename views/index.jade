extends layout

block script
	//something goes here
block content
	include navbar
	
	#searchBar
		h1 UPRMbooks
		h3 Search for textbooks by ISBN, title o author
		.row
			.col-md-6.col-md-offset-3
			form.navbar-form.navbar-left(role="search", action='/search', method='post', name='search', enctype="multipart/form-data")
				.form-group
					input#srch-term.form-control(type='text', placeholder='Search', name='query', required)
				button.btn.btn-default(type='submit')
					i.glyphicon.glyphicon-search
	script.
		$(document).ready(function(){
			$('#topsearch').hide();
		})

//- *0.90*0.151